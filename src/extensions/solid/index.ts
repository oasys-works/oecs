/**
 * `@oasys/oecs/solid` — the SolidJS adapter plugin for oecs's reactive
 * kernel (`@oasys/oecs/reactive` / `../../reactive`).
 */
export {
	fromKernel,
	fromKernelMap,
	fromKernelStruct,
	fromKernelArray,
	type KernelMapView
} from "./kernel_solid";
